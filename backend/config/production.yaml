# Production Overrides
env: "production"
database_url: "${DATABASE_URL}"  # Set via environment variable or secret manager
secret_key: "${SECRET_KEY}"  # Set via environment variable or secret manager
debug: false
log_level: "INFO"
